apiVersion: v1
kind: Service
metadata:
  name: {{ project_name }}-service
  namespace: {{ namespace }}
spec:
  selector:
    app: {{ project_name }}
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: NodePort
